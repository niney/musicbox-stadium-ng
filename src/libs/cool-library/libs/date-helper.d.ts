/**
 * DateHelper
 *
 * moment 라이브러리에 의존함
 */
export declare class DateHelper {
    /**
     * date 를 하루의 끝시간으로 설정
     * @param {string} dateStr
     * @returns {string}
     */
    setEndDateToISOString(dateStr: string): any;
}
